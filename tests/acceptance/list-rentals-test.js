import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | list rentals', function(hooks) {
  setupApplicationTest(hooks);

    test('should show rentals as the home page', async function(assert){
        
    });
    test('should link to the information about the company', async function(assert){
        
    });
    test('should link to contact information', async function(assert){
        
    });
    test('should list avaliable rentals', async function(assert){
        
    });
    test('should filter the avaliable rentals by city', async function(assert){
        
    });
    test('should show information about the rental', async function(assert){
        
    });
    
});
